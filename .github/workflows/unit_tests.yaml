name: Unit tests
on: [push]

jobs:
  build:
    name: Build source
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2-beta
      - uses: actions/setup-go@v2-beta
        with:
          go-version: '1.14'
      - run: go build
